// src/lib/ai/context-builder.ts
export async function buildContext(userId: string): Promise<string> {
  // Placeholder context assembly for user prompt enhancement
  return `UserContext:${userId}`;
}
