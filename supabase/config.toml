# Supabase configuration for Futuril

# Replace with your Supabase project ID if different
project_id = "ljzrovpkklsklypszxdg"

# Optional: set the path to migrations (default is ./supabase/migrations)
[migrations]
# directory = "supabase/migrations"
